name: Publish wiki

on:
  push:
    branches: [master]
    paths: ["docs/wiki/**", ".github/workflows/wiki.yml"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Clone the wiki repository
        run: |
          git clone "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.wiki.git" wiki \
            || { echo "::error::The wiki has no pages yet. Create the first page at https://github.com/${{ github.repository }}/wiki, then re-run this workflow."; exit 1; }
      - name: Copy pages
        run: rsync -a --delete --exclude .git docs/wiki/ wiki/
      - name: Commit and push
        working-directory: wiki
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then echo "Wiki already up to date"; exit 0; fi
          git commit -m "Sync from docs/wiki @ ${{ github.sha }}"
          git push
